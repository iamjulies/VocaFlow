<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Thần Thoại Hồng Căn Tâm Chuẩn 360°</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #060207; /* Nền tối sâu làm nổi bật ánh neon */
            margin: 0;
            font-family: sans-serif;
            overflow: hidden;
        }

        /* Container tổng thể - neo giữ mọi phần tử tại tâm */
        .avatar-container {
            position: relative;
            width: 170px;
            height: 170px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* 1. HIỆU ỨNG HÀO QUANG TOẢ RỘNG (AURA) - CĂN TÂM CHUẨN */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 180px;
            height: 180px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(255, 0, 127, 0.6) 0%, rgba(255, 105, 180, 0.2) 50%, rgba(255, 20, 147, 0) 75%);
            filter: blur(30px);
            z-index: 0;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) scale(1);
            animation: aura-expand-fixed 2s ease-in-out infinite alternate;
        }

        @keyframes aura-expand-fixed {
            0% { transform: translate(-50%, -50%) scale(0.85); opacity: 0.5; }
            100% { transform: translate(-50%, -50%) scale(1.2); opacity: 1; }
        }

        /* 2. HIỆU ỨNG HẠT TUNG TÓE (SPLATTER) - KHẮC PHỤC LỖI LỆCH TÂM */
        .particles-splatter {
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            z-index: 0;
            pointer-events: none;
        }
        
        .particles-splatter::before,
        .particles-splatter::after {
            content: '';
            position: absolute;
            width: 6px;
            height: 6px;
            border-radius: 50%;
            /* Đưa điểm neo gốc về đúng tâm 0,0 */
            top: -3px;
            left: -3px; 
            transform-origin: 0 0;
            animation: splatter-orbit-fixed 6s linear infinite;
        }

        /* Hệ thống các đốm lửa ma thuật đối xứng xung quanh tâm */
        .particles-splatter::before {
            background: #ff1493;
            box-shadow: 
                -40px -70px 4px #ff007f, 70px -50px 2px #ff69b4, 
                -80px 30px 6px #ff1493, 90px 40px 3px #ffccd5,
                -30px 90px 5px #ff007f, 5px -100px 4px #ff69b4,
                80px -90px 3px #ff007f, -90px -60px 5px #ffccd5;
        }

        .particles-splatter::after {
            background: #ffffff;
            box-shadow: 
                -60px -40px 2px #ff69b4, 50px -80px 4px #ff007f, 
                -100px -10px 3px #ff1493, 70px 80px 5px #ffccd5,
                -70px 70px 2px #ff007f, 40px 100px 4px #ff69b4,
                -10px -110px 3px #ffffff, 110px -20px 2px #ff1493;
            animation-direction: reverse;
            animation-duration: 4s;
        }

        @keyframes splatter-orbit-fixed {
            0% { transform: rotate(0deg) scale(0.95); }
            100% { transform: rotate(360deg) scale(0.95); }
        }

        /* 3. KHUNG CHÍNH CONIC Xoay 360° */
        .conic-mythic-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: conic-gradient(
                from 0deg, 
                #ff74a4, 
                #ff1493, 
                #ff8da1, 
                #ff007f, 
                #ffb6c1, 
                #ff1493, 
                #ff74a4
            );
            padding: 11px;
            box-sizing: border-box;
            z-index: 1;
            box-shadow: 0 0 30px rgba(255, 20, 147, 0.7), inset 0 2px 5px rgba(255,255,255,0.9);
            animation: rotate-360 3.5s linear infinite;
        }

        @keyframes rotate-360 {
            100% { transform: rotate(360deg); }
        }

        /* Lõi chứa ảnh avatar (Triệt tiêu gia tốc xoay để ảnh đứng thăng bằng) */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #120713;
            border: 2px solid #ff69b4;
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: anti-rotate-360 3.5s linear infinite;
        }

        @keyframes anti-rotate-360 {
            100% { transform: rotate(-360deg); }
        }

        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* ĐOÂN CÁNH THẦN THOẠI - Đã căn cố định vào hai biên biên khung */
        .mythic-wing {
            position: absolute;
            font-size: 26px;
            z-index: 3;
            top: 50%;
            margin-top: -18px; /* Đưa biên dọc về chuẩn tâm cánh */
            filter: drop-shadow(0 0 12px rgba(255, 105, 180, 1));
        }

        .wing-left {
            left: -20px;
            transform: scaleX(-1);
            animation: wing-float-left 2s ease-in-out infinite alternate;
        }

        .wing-right {
            right: -20px;
            animation: wing-float-right 2s ease-in-out infinite alternate;
        }

        @keyframes wing-float-left {
            0% { transform: scaleX(-1) translateY(0px); }
            100% { transform: scaleX(-1) translateY(-6px); }
        }

        @keyframes wing-float-right {
            0% { transform: translateY(0px); }
            100% { transform: translateY(-6px); }
        }

        /* HUY HIỆU Ở ĐÁY KHUNG - Căn trục ngang chuẩn chỉ */
        .mythic-badge {
            position: absolute;
            bottom: -10px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, #ffffff, #ff69b4, #ff1493);
            border: 2px solid #ffffff;
            padding: 3px 18px;
            border-radius: 30px;
            z-index: 4;
            box-shadow: 0 4px 20px rgba(255, 20, 147, 0.8);
            display: flex;
            gap: 3px;
            align-items: center;
        }

        .mythic-badge span {
            font-size: 11px;
            color: #ffffff;
            text-shadow: 0 0 6px #ff007f, 0 1px 2px rgba(0,0,0,0.8);
        }
    </style>
</head>
<body>

    <div class="avatar-container">
        <!-- Hệ thống hạt bung tỏa xoay quanh tâm chuẩn -->
        <div class="particles-splatter"></div>

        <!-- Đôi cánh thần thoại phát sáng -->
        <div class="mythic-wing wing-left">🪽</div>
        <div class="mythic-wing wing-right">🪽</div>

        <!-- Vòng viền Conic 360 độ quay mượt mà -->
        <div class="conic-mythic-frame">
            <div class="avatar-inside">
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>

        <!-- Ruy băng huy hiệu đáy -->
        <div class="mythic-badge">
            <span>✨</span>
            <span>★</span>
            <span>★</span>
            <span>★</span>
            <span>✨</span>
        </div>
    </div>

</body>
</html>
