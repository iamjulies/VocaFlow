<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Đồng Sơ Cấp</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #121214; /* Nền tối để làm nổi bật khung đồng */
            margin: 0;
            font-family: sans-serif;
        }

        /* Container chứa toàn bộ avatar và khung */
        .avatar-container {
            position: relative;
            width: 150px;
            height: 150px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Hiệu ứng hào quang phát sáng nhẹ màu đồng phía sau */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 140px;
            height: 140px;
            border-radius: 50%;
            background: #cd7f32;
            filter: blur(20px);
            opacity: 0.25;
            z-index: 0;
        }

        /* Khung viền Đồng (Bronze Frame) bằng gradient */
        .bronze-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            /* Gradient giả lập chất liệu kim loại đồng ánh kim */
            background: linear-gradient(135deg, #a36224 0%, #e59851 25%, #8c4f1a 50%, #f3b06d 75%, #703c11 100%);
            padding: 8px; /* Độ dày của khung viền đồng */
            box-sizing: border-box;
            z-index: 1;
            box-shadow: inset 0 2px 4px rgba(255,255,255,0.4), 0 4px 15px rgba(0,0,0,0.6);
        }

        /* Phần lõi bên trong để chứa ảnh avatar */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #222; /* Màu nền dự phòng nếu ảnh lỗi */
            border: 2px solid #3e2007; /* Viền mỏng ngăn cách ảnh và khung */
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Ảnh người dùng */
        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Huy hiệu Rank Sơ Cấp gắn dưới khung viền */
        .bronze-badge {
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, #e59851, #8c4f1a);
            border: 2px solid #f3b06d;
            border-radius: 6px;
            rotate: 45deg; /* Xoay hình vuông tạo thành hình thoi */
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 2;
            box-shadow: 0 3px 6px rgba(0,0,0,0.5);
        }

        /* Ngôi sao nhỏ bên trong huy hiệu sơ cấp */
        .bronze-badge::after {
            content: '★';
            font-size: 14px;
            color: #fff;
            text-shadow: 0 1px 2px rgba(0,0,0,0.5);
            rotate: -45deg; /* Xoay ngược lại để ngôi sao đứng thẳng */
        }
    </style>
</head>
<body>

    <!-- Khối Avatar -->
    <div class="avatar-container">
        <!-- Khung đồng -->
        <div class="bronze-frame">
            <!-- Ảnh bên trong (Thay link ảnh của bạn vào thẻ src nhé) -->
            <div class="avatar-inside">
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>
        <!-- Huy hiệu Sơ Cấp (1 Sao) -->
        <div class="bronze-badge"></div>
    </div>

</body>
</html>
