<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Hoàng Triều Vĩnh Cửu (VIP Lifetime)</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #08060a; /* Nền tối ánh tím nhẹ tôn vinh sắc hồng hoàng gia */
            margin: 0;
            font-family: system-ui, -apple-system, sans-serif;
            overflow: hidden;
        }

        /* Container tổng thể - giữ mọi lớp layer đồng tâm tuyệt đối */
        .avatar-container {
            position: relative;
            width: 170px;
            height: 170px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 25px; /* Tạo không gian cho vương miện phía trên */
        }

        /* 1. HIỆU ỨNG HÀO QUANG HOÀNG TRIỀU PHÁT SÁNG (ROYAL PASTEL GLOW) */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 185px;
            height: 185px;
            border-radius: 50%;
            /* Hào quang kết hợp hồng pastel dịu ngọt và ánh vàng kim quý phái */
            background: radial-gradient(circle, rgba(255, 182, 193, 0.4) 0%, rgba(212, 175, 55, 0.15) 55%, rgba(0, 0, 0, 0) 75%);
            filter: blur(20px);
            z-index: 0;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation: royal-pulse 3s ease-in-out infinite alternate;
        }

        @keyframes royal-pulse {
            0% { transform: translate(-50%, -50%) scale(0.94); opacity: 0.5; }
            100% { transform: translate(-50%, -50%) scale(1.08); opacity: 0.9; }
        }

        /* 2. ĐỈNH AVATAR: BIỂU TƯỢNG VƯƠNG MIỆN 3D NẢY NHẸ (3D ROYAL CROWN) */
        .royal-crown-3d {
            position: absolute;
            top: -24px;
            font-size: 32px;
            z-index: 4;
            filter: drop-shadow(0 4px 8px rgba(212, 175, 55, 0.5));
            animation: crown-bounce-subtle 2.2s ease-in-out infinite;
            transform-origin: bottom center;
        }

        @keyframes crown-bounce-subtle {
            0%, 100% { transform: translateY(0) scale(1); }
            50% { transform: translateY(-5px) scale(1.04) rotate(2deg); } /* Nảy nhẹ kèm độ nghiêng hoàng gia tinh tế */
        }

        /* 3. KHUNG CHÍNH HOÀNG TRIỀU VĨNH CỬU - CHUYỂN gradient XOAY VÒNG CHU VI */
        .royal-lifetime-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            /* Phối dải màu hoàng gia: vàng kim sâm banh, trắng bạch kim và hồng phấn pastel mượt mà */
            background: conic-gradient(
                from 0deg,
                #ffd700 0%,     /* Vàng kim 24K */
                #ffb6c1 20%,    /* Hồng phấn Pastel */
                #fff5c0 40%,    /* Champagne Gold */
                #ffffff 50%,    /* Bạch kim bắt sáng */
                #ffb6c1 70%,    /* Hồng phấn Pastel */
                #bf953f 90%,    /* Vàng kim cổ điển */
                #ffd700 100%
            );
            padding: 9px; /* Độ dày viền VIP hoàng gia */
            box-sizing: border-box;
            z-index: 1;
            box-shadow: 0 10px 25px rgba(0,0,0,0.5), inset 0 2px 4px rgba(255,255,255,0.7);
            animation: royal-rotate 5s linear infinite;
        }

        @keyframes royal-rotate {
            100% { transform: rotate(360deg); }
        }

        /* LÕI CHỨA AVATAR (Triệt tiêu lực xoay của khung ngoài giúp ảnh đứng thăng bằng) */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #0f0b14;
            border: 2px solid #ffb6c1; /* Đường viền trong màu hồng pastel đồng điệu */
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: anti-royal-rotate 5s linear infinite;
        }

        @keyframes anti-royal-rotate {
            100% { transform: rotate(-360deg); }
        }

        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* 4. HUY HIỆU ĐÁY: KHẲNG ĐỊNH ĐẲNG CẤP VIP LIFETIME */
        .royal-vip-badge {
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            /* Phối màu dốc hoàng kim vĩnh cửu */
            background: linear-gradient(135deg, #fff5c0, #ffd700, #bf953f);
            border: 1.5px solid #ffffff;
            padding: 3px 18px;
            border-radius: 20px; /* Kiểu ruy-băng bo tròn cao cấp */
            z-index: 4;
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Chữ hiển thị phân hạng đặc quyền */
        .royal-vip-badge span {
            font-size: 10px;
            font-weight: 900;
            color: #4a3200; /* Chữ sẫm màu đè trên nền vàng bắt sáng cực sang trọng */
            letter-spacing: 1.5px;
            text-shadow: 0 0.5px 1px rgba(255,255,255,0.6);
        }
    </style>
</head>
<body>

    <div class="avatar-container">
        <!-- Biểu tượng vương miện 3D nảy nhẹ điệu đà trên đỉnh -->
        <div class="royal-crown-3d">👑</div>

        <!-- Khung viền dải lụa hoàng gia xoay tròn mịn màng -->
        <div class="royal-lifetime-frame">
            <div class="avatar-inside">
                <!-- Đường dẫn ảnh đại diện người dùng -->
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>

        <!-- Thanh danh hiệu VIP vĩnh viễn ở đáy -->
        <div class="royal-vip-badge">
            <span>VIP LIFETIME</span>
        </div>
    </div>

</body>
</html>
