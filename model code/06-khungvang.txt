<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Vàng Hoàng Gia (Gold Shimmer)</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #0b0b0d; /* Nền siêu tối để tôn vinh sắc vàng hoàng gia */
            margin: 0;
            font-family: sans-serif;
        }

        /* Container tổng thể */
        .avatar-container {
            position: relative;
            width: 160px;
            height: 160px;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-top: 20px;
        }

        /* Hào quang vàng quyền lực tỏa sáng phía sau */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 145px;
            height: 145px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(255,215,0,0.4) 0%, rgba(212,175,55,0) 70%);
            filter: blur(15px);
            z-index: 0;
            animation: pulse-glow 3s ease-in-out infinite alternate;
        }

        @keyframes pulse-glow {
            0% { transform: scale(0.95); opacity: 0.5; }
            100% { transform: scale(1.05); opacity: 0.9; }
        }

        /* Biểu tượng Vương Miện VIP phía trên khung với hiệu ứng nảy (Bounce) */
        .gold-crown {
            position: absolute;
            top: -22px;
            font-size: 28px;
            z-index: 3;
            filter: drop-shadow(0 3px 6px rgba(0,0,0,0.5));
            animation: crown-bounce 2s ease-in-out infinite;
        }

        @keyframes crown-bounce {
            0%, 100% { transform: translateY(0) scale(1); }
            50% { transform: translateY(-6px) scale(1.05); }
        }

        /* Khung viền Vàng Hoàng Gia tạo lớp */
        .gold-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            /* Gradient vàng đa tầng tạo chiều sâu kim loại đúc */
            background: linear-gradient(135deg, #bf953f 0%, #fcf6ba 25%, #b38728 50%, #fbf5b7 75%, #aa771c 100%);
            padding: 10px; /* Độ dày viền vàng */
            box-sizing: border-box;
            z-index: 1;
            overflow: hidden; /* Cần thiết để cắt hiệu ứng shimmer chạy qua */
            box-shadow: 0 10px 25px rgba(0,0,0,0.6), inset 0 2px 4px rgba(255,255,255,0.6);
        }

        /* HIỆU ỨNG SHIMMER: Dải sáng lướt qua bề mặt vàng quý phái */
        .gold-frame::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -60%;
            width: 30%;
            height: 200%;
            background: linear-gradient(
                to right,
                rgba(255, 255, 255, 0) 0%,
                rgba(255, 255, 255, 0.6) 50%,
                rgba(255, 255, 255, 0) 100%
            );
            transform: rotate(30deg);
            animation: shimmer-flow 3.5s cubic-bezier(0.4, 0, 0.2, 1) infinite;
        }

        @keyframes shimmer-flow {
            0% { left: -60%; }
            30%, 100% { left: 140%; } /* Chạy qua rồi nghỉ một nhịp ngắn trước khi lặp lại */
        }

        /* Lõi chứa ảnh avatar */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #1a1a1f;
            border: 2px solid #8a6623; /* Viền trong màu vàng sậm */
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Huy hiệu Danh Hiệu Hoàng Gia đính góc dưới với 3 Ngôi sao tinh tú */
        .gold-badge {
            position: absolute;
            bottom: -5px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, #fcf6ba, #bf953f);
            border: 2px solid #ffffff;
            padding: 2px 14px;
            border-radius: 20px;
            z-index: 3;
            box-shadow: 0 4px 12px rgba(255,215,0,0.3);
            display: flex;
            gap: 3px;
        }

        .gold-badge span {
            font-size: 12px;
            color: #ffffff;
            text-shadow: 0 1px 3px rgba(0,0,0,0.8), 0 0 4px #b38728;
        }
    </style>
</head>
<body>

    <div class="avatar-container">
        <!-- Biểu tượng vương miện nảy điệu đà -->
        <div class="gold-crown">👑</div>

        <!-- Khung vàng hoàng gia có hiệu ứng quét sáng -->
        <div class="gold-frame">
            <div class="avatar-inside">
                <!-- Thay đổi đường dẫn ảnh của bạn tại đây -->
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>

        <!-- Huy hiệu hoàng gia 3 sao -->
        <div class="gold-badge">
            <span>★</span>
            <span>★</span>
            <span>★</span>
        </div>
    </div>

</body>
</html>
