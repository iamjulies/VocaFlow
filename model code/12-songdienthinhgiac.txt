<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Sóng Điện Thính Giác (Dictation Lab)</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #040108; /* Nền tối đen ánh tím sâu để tôn vinh sắc tím Neon */
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            overflow: hidden;
        }

        /* Container tổng thể - giữ cố định mọi tầng layer tại tâm */
        .avatar-container {
            position: relative;
            width: 170px;
            height: 170px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* 1. HÀO QUANG XUNG ĐIỆN TÍM TOẢ RỘNG (AURA GLOW) */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 185px;
            height: 185px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(189, 0, 255, 0.5) 0%, rgba(0, 242, 254, 0.1) 50%, rgba(0, 0, 0, 0) 75%);
            filter: blur(25px);
            z-index: 0;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation: audio-pulse 2s ease-in-out infinite alternate;
        }

        @keyframes audio-pulse {
            0% { transform: translate(-50%, -50%) scale(0.9); opacity: 0.4; }
            100% { transform: translate(-50%, -50%) scale(1.1); opacity: 0.9; }
        }

        /* 2. CÁC CỘT XUNG ĐIỆN ÂM THANH NGOẠI VI (AUDIO VISUALIZER BARS) */
        .audio-waves {
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: 3;
            pointer-events: none;
        }

        /* Giả lập các thanh sóng nhạc nhô lên co giãn từ biên viền */
        .audio-waves::before,
        .audio-waves::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            border-radius: 2px;
            background: #00f2fe; /* Kết hợp ánh xanh neon tương phản cực mạnh */
            box-shadow: 0 0 10px #00f2fe, 0 0 20px #bd00ff;
        }

        /* Cột sóng bên trái và bên phải */
        .audio-waves::before {
            width: 194px;
            height: 4px;
            transform: translate(-50%, -50%);
            animation: wave-bounce-horizontal 0.6s ease-in-out infinite alternate;
        }

        /* Cột sóng bên trên và bên dưới */
        .audio-waves::after {
            width: 4px;
            height: 194px;
            transform: translate(-50%, -50%);
            animation: wave-bounce-vertical 0.8s ease-in-out infinite alternate-reverse;
        }

        @keyframes wave-bounce-horizontal {
            0% { transform: translate(-50%, -50%) scaleX(0.92); opacity: 0.7; }
            100% { transform: translate(-50%, -50%) scaleX(1.04); opacity: 1; }
        }

        @keyframes wave-bounce-vertical {
            0% { transform: translate(-50%, -50%) scaleY(0.94); opacity: 0.6; }
            100% { transform: translate(-50%, -50%) scaleY(1.05); opacity: 1; }
        }

        /* 3. KHUNG CHÍNH TÍM NEON Xoay 360° CHẠY QUANH CHU VI */
        .audio-purple-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            /* Hiệu ứng tia chớp điện quét vòng tròn */
            background: conic-gradient(
                from 0deg, 
                #bd00ff 0%,     /* Hồng tím Neon rực cháy */
                #00f2fe 25%,    /* Điểm giao thoa điện xanh */
                rgba(189, 0, 255, 0.3) 50%, 
                rgba(0, 242, 254, 0.1) 70%,
                #bd00ff 100%
            );
            padding: 8px; /* Độ dày viền thính giác */
            box-sizing: border-box;
            z-index: 1;
            box-shadow: 0 0 25px rgba(189, 0, 255, 0.6), inset 0 0 12px rgba(0, 242, 254, 0.5);
            animation: perimeter-scan 2.5s linear infinite;
        }

        @keyframes perimeter-scan {
            100% { transform: rotate(360deg); }
        }

        /* LÕI CHỨA AVATAR (Triệt tiêu lực xoay) */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #0b0414;
            border: 2px solid rgba(189, 0, 255, 0.5); 
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: anti-perimeter-scan 2.5s linear infinite;
        }

        @keyframes anti-perimeter-scan {
            100% { transform: rotate(-360deg); }
        }

        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: saturate(1.2) contrast(1.05);
        }

        /* 4. HUY HIỆU ĐÁY: BẢNG TẦN SỐ ÂM THANH (AUDIO BADGE) */
        .audio-frequency-badge {
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, #0f051d, #240046);
            border: 1.5px solid #00f2fe;
            padding: 2px 16px;
            border-radius: 20px; /* Dáng bo góc dạng viên nhộng mềm mại */
            z-index: 4;
            box-shadow: 0 4px 15px rgba(189, 0, 255, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Ký tự hoặc tên tính năng hiển thị */
        .audio-frequency-badge span {
            font-size: 10px;
            font-weight: bold;
            color: #ffffff;
            letter-spacing: 1.5px;
            text-shadow: 0 0 5px #00f2fe, 0 0 10px #bd00ff;
        }
    </style>
</head>
<body>

    <div class="avatar-container">
        <!-- Hệ thống cột sóng âm thanh co giãn ngoại vi -->
        <div class="audio-waves"></div>

        <!-- Khung viền xung điện quét chu vi tròn 360 độ -->
        <div class="audio-purple-frame">
            <div class="avatar-inside">
                <!-- Đường dẫn ảnh đại diện -->
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>

        <!-- Huy hiệu tần số thính giác ở đáy -->
        <div class="audio-frequency-badge">
            <span>DICTATION LAB</span>
        </div>
    </div>

</body>
</html>
