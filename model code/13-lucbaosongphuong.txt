<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Lục Bảo Song Phương (Translation Lab)</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #040807; /* Nền tối sâu thẳm giúp sắc ngọc lục bảo và xanh lam phát sáng rực rỡ */
            margin: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            overflow: hidden;
        }

        /* Container tổng thể - giữ mọi lớp layer đồng tâm tuyệt đối */
        .avatar-container {
            position: relative;
            width: 170px;
            height: 170px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* 1. HIỆU ỨNG HÀO QUANG SONG PHƯƠNG (DUAL AURA GLOW) */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 185px;
            height: 185px;
            border-radius: 50%;
            /* Sự kết hợp hào quang đối lập giữa xanh lục bảo và xanh lam */
            background: radial-gradient(circle, rgba(16, 185, 129, 0.4) 0%, rgba(59, 130, 246, 0.2) 50%, rgba(0, 0, 0, 0) 75%);
            filter: blur(25px);
            z-index: 0;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            animation: dual-pulse 3s ease-in-out infinite alternate;
        }

        @keyframes dual-pulse {
            0% { transform: translate(-50%, -50%) scale(0.92); opacity: 0.5; }
            100% { transform: translate(-50%, -50%) scale(1.08); opacity: 0.9; }
        }

        /* 2. KHUNG CHÍNH LỤC BẢO SONG PHƯƠNG - PHÂN CHIA HAI NỬA TƯƠNG PHẢN ĐỐI XỨNG */
        .translation-emerald-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            /* Tạo dải màu phân chia 2 nửa rõ rệt nhưng chuyển vân bắt sáng mượt mà */
            background: conic-gradient(
                from 0deg,
                #10b981 0%,   /* Bắt đầu nửa trên: Xanh lục bảo Emerald */
                #059669 25%,
                #10b981 48%,
                #ffffff 50%,  /* Điểm giao thoa sáng */
                #3b82f6 52%,  /* Bắt đầu nửa dưới: Xanh lam Ocean */
                #2563eb 75%,
                #3b82f6 98%,
                #ffffff 100%
            );
            padding: 9px; /* Độ dày viền */
            box-sizing: border-box;
            z-index: 1;
            box-shadow: 0 0 25px rgba(16, 185, 129, 0.4), 0 0 25px rgba(59, 130, 246, 0.4), inset 0 2px 4px rgba(255,255,255,0.6);
            animation: twin-scan 4s linear infinite;
        }

        @keyframes twin-scan {
            100% { transform: rotate(360deg); }
        }

        /* LÕI CHỨA AVATAR (Triệt tiêu gia tốc xoay để ảnh đứng thăng bằng) */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #070e12;
            border: 2px solid rgba(255, 255, 255, 0.15);
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: anti-twin-scan 4s linear infinite;
        }

        @keyframes anti-twin-scan {
            100% { transform: rotate(-360deg); }
        }

        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* 3. HUY HIỆU ĐÁY: VÒNG XOAY CHUYỂN NGỮ (TRANSLATION BADGE) */
        .translation-badge {
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            /* Phối màu gradient hòa hợp giữa 2 thế giới Lục bảo và Xanh lam */
            background: linear-gradient(135deg, #10b981, #3b82f6);
            border: 1.5px solid #ffffff;
            padding: 3px 14px;
            border-radius: 20px;
            z-index: 4;
            box-shadow: 0 4px 15px rgba(16, 185, 129, 0.5);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        /* Biểu tượng chuyển đổi ngôn ngữ thu nhỏ */
        .translation-badge::before {
            content: '⇄';
            font-size: 11px;
            color: #ffffff;
            font-weight: bold;
            animation: arrow-flicker 1.5s ease-in-out infinite alternate;
        }

        @keyframes arrow-flicker {
            0% { opacity: 0.5; transform: scale(0.9); }
            100% { opacity: 1; transform: scale(1.1); }
        }

        /* Chữ hiển thị tên tính năng sắc nét */
        .translation-badge span {
            font-size: 10px;
            font-weight: bold;
            color: #ffffff;
            letter-spacing: 1px;
            text-shadow: 0 1px 2px rgba(0,0,0,0.5);
        }
    </style>
</head>
<body>

    <div class="avatar-container">
        <!-- Khung viền ngọc lục bảo & xanh lam đối xứng xoay tròn 360 độ -->
        <div class="translation-emerald-frame">
            <div class="avatar-inside">
                <!-- Đường dẫn ảnh đại diện -->
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>

        <!-- Huy hiệu Song Phương ở đáy -->
        <div class="translation-badge">
            <span>TRANSLATION LAB</span>
        </div>
    </div>

</body>
</html>
