<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khung Avatar Lam Ngọc (Diamond Glow)</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #050811; /* Nền tối sâu thẳm kiểu không gian để làm nổi bật ánh sáng lam ngọc */
            margin: 0;
            font-family: sans-serif;
        }

        /* Container tổng thể */
        .avatar-container {
            position: relative;
            width: 165px;
            height: 165px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Hiệu ứng hào quang Lam Ngọc phát sáng (Neon Glow) dạng tỏa mờ */
        .avatar-container::before {
            content: '';
            position: absolute;
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(0, 242, 254, 0.45) 0%, rgba(79, 172, 254, 0) 70%);
            filter: blur(20px);
            z-index: 0;
            animation: diamond-pulse 2.5s ease-in-out infinite alternate;
        }

        @keyframes diamond-pulse {
            0% { transform: scale(0.96); opacity: 0.6; }
            100% { transform: scale(1.06); opacity: 1; }
        }

        /* Khung viền Lam Ngọc với luồng sáng chạy vòng tròn (Conic Gradient) */
        .diamond-frame {
            position: absolute;
            width: 100%;
            height: 100%;
            border-radius: 50%;
            /* Phối các dải sắc độ xanh ngọc, xanh lục bảo và trắng kim cương xoay vòng */
            background: conic-gradient(from 0deg, #00f2fe, #4facfe, #00f2fe, #ffffff, #4facfe, #00f2fe);
            padding: 10px; /* Độ dày của khung */
            box-sizing: border-box;
            z-index: 1;
            box-shadow: 0 0 20px rgba(0, 242, 254, 0.4), inset 0 2px 4px rgba(255,255,255,0.7);
            animation: rotate-glow 5s linear infinite;
        }

        @keyframes rotate-glow {
            100% { transform: rotate(360deg); }
        }

        /* Lõi chứa ảnh avatar (Triệt tiêu lực xoay của khung ngoài giúp ảnh đứng thẳng) */
        .avatar-inside {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background-color: #0b111e;
            border: 2px solid #00f2fe; /* Đường viền mỏng neon ngăn cách */
            box-sizing: border-box;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            animation: anti-rotate-glow 5s linear infinite;
        }

        @keyframes anti-rotate-glow {
            100% { transform: rotate(-360deg); }
        }

        .avatar-inside img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Huy hiệu Tinh Thể Lam Ngọc đính ở đáy khung */
        .diamond-badge {
            position: absolute;
            bottom: -8px;
            left: 50%;
            transform: translateX(-50%);
            background: linear-gradient(135deg, #ffffff, #00f2fe, #4facfe);
            border: 2px solid #ffffff;
            padding: 3px 16px;
            border-radius: 30px;
            z-index: 3;
            box-shadow: 0 4px 15px rgba(0, 242, 254, 0.5);
            display: flex;
            gap: 4px;
            align-items: center;
        }

        /* Biểu tượng kim cương lấp lánh */
        .diamond-badge::before {
            content: '💎';
            font-size: 13px;
            margin-right: 2px;
            filter: drop-shadow(0 1px 2px rgba(0,0,0,0.3));
        }

        /* Các ngôi sao tinh tú tỏa sáng rực rỡ */
        .diamond-badge span {
            font-size: 11px;
            color: #ffffff;
            text-shadow: 0 0 5px #00f2fe, 0 1px 2px rgba(0,0,0,0.8);
        }
    </style>
</head>
<body>

    <div class="avatar-container">
        <!-- Khung lam ngọc huyền ảo chuyển động xoay tròn -->
        <div class="diamond-frame">
            <!-- Ảnh bên trong giữ thăng bằng ổn định -->
            <div class="avatar-inside">
                <img src="https://dicebear.com" alt="Avatar">
            </div>
        </div>

        <!-- Huy hiệu Tối Thượng đính kèm biểu tượng kim cương -->
        <div class="diamond-badge">
            <span>★</span>
            <span>★</span>
            <span>★</span>
            <span>★</span>
        </div>
    </div>

</body>
</html>
